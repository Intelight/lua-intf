machine:
  services:
    - docker

#dependencies:
#  override:
#    - sudo pip install --upgrade docker-compose

compile:
  pre:
    - docker --version
    - ls -la
    - pwd
    - cd LuaIntf && pwd
    - pwd
    - cat /etc/os-release
  override:
    - docker-compose --version